#!/bin/bash
set -e

if [ "$1" = "configure" ]; then
  # Ensure scripts are executable
  chmod +x src/usr/local/bin/auto-install 2>/dev/null || true

  echo "VyOS customization package configured successfully"
fi

#DEBHELPER#

exit 0
